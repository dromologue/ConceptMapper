Run all tests across the project:

1. `cargo test --all` — Rust parser and integration tests
2. `cd web && npm test` — Frontend Vitest tests

Report results for each. If any fail, show the failure output and suggest a fix.
